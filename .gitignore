node_modules/
.env

# data/ holds settings.json (store name/admin password) — still local disk,
# see the "Known gaps" section of the README. files/ and uploads/ are dead
# now that master PDFs, covers and rendered pages all live in R2, kept
# ignored in case anything ever writes there locally again.
data/
files/
uploads/
